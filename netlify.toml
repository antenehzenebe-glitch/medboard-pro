[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

[functions."generate-mcq"]
  timeout = 26

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
